# Workspace Notes

This workspace is a TypeScript full-stack scaffold. Keep frontend code in `client`, backend code in `server`, and database definitions in `server/prisma`.

Assignment business logic is intentionally not included in the scaffold.
